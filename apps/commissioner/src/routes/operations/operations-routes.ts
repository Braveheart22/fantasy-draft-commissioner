import type {FastifyInstance} from "fastify"; import type {BackupCoordinator} from "../../infrastructure/files/backup-coordinator.js"; import type {CorrectionService} from "../../application/corrections/correction-service.js"; import {CORRECTION_TYPES,type CorrectionType} from "../../application/corrections/correction-types.js"; import type {RecoveryService} from "../../application/recovery/recovery-service.js";
export async function registerOperationsRoutes(server:FastifyInstance,services:{backup:BackupCoordinator;corrections:CorrectionService;recovery:RecoveryService;backupDirectory:string}){
 server.get("/api/operations/recovery",()=>services.recovery.summary());
 server.post<{Body:{seasonId?:string;seasonVersion?:number;destinationDirectory?:string;trigger?:string}}>("/api/operations/backups",request=>services.backup.create(request.body.destinationDirectory??services.backupDirectory,{...(request.body.seasonId?{seasonId:request.body.seasonId}:{}),...(request.body.seasonVersion!==undefined?{seasonVersion:request.body.seasonVersion}:{}),trigger:request.body.trigger??"MANUAL"}));
 server.post<{Body:{manifestPath:string}}>("/api/operations/backups/verify",request=>services.backup.verify(request.body.manifestPath));
 server.post<{Params:{seasonId:string};Body:{correctionType:CorrectionType;targetId?:string}}>("/api/operations/:seasonId/corrections/preview",request=>{if(!CORRECTION_TYPES.includes(request.body.correctionType))throw new Error("Unsupported correction type");return services.corrections.preview(request.params.seasonId,request.body.correctionType,request.body.targetId);});
 server.post<{Params:{previewId:string};Body:{expectedVersion:number;cutHash:string;backupHash:string;confirmation:string;reason:string}}>("/api/operations/corrections/:previewId/confirm",request=>services.corrections.confirm(request.params.previewId,request.body));
}
