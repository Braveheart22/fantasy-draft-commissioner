import React,{useState} from "react";
export function OperationsPanel({seasonId}){const [receipt,setReceipt]=useState(null);const [error,setError]=useState("");async function backup(){setError("");try{const response=await fetch("/api/operations/backups",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({seasonId})});if(!response.ok)throw new Error(await response.text());setReceipt(await response.json());}catch(value){setError(String(value));}}return <section aria-labelledby="operations-title"><h2 id="operations-title">Backup and recovery</h2><p>Create a verified local checkpoint before high-risk changes.</p><button type="button" onClick={backup}>Create verified backup</button>{receipt&&<p role="status">Verified backup: <code>{receipt.sha256}</code></p>}{error&&<p role="alert">{error}</p>}</section>}
